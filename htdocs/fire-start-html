<!DOCTYPE html>
<html>
<head>
    <style>
        body { margin: 0; padding: 0; overflow: hidden; font-family: monospace; }
        #taskbar { width: 100%; height: 25px; background: #001A33; border-top: 1px solid #FFD700; display: flex; }
        #start-btn { background: #FFD700; color: #000; border: none; padding: 0 15px; font-weight: bold; cursor: pointer; }
        #menu { 
            display: none; position: fixed; bottom: 25px; left: 0; 
            width: 200px; background: #002244; border: 1px solid #FFD700; 
        }
        .item { color: #FFD700; padding: 10px; cursor: pointer; border-bottom: 1px solid #003366; }
        .item:hover { background: #FFD700; color: #001A33; }
    </style>
</head>
<body>
    <div id="menu">
        <div class="item" onclick="parent.createWindow('BYETHOST', 'https://mercwar.byethost3.com')">> AVIS Surface</div>
        <div class="item" onclick="parent.createWindow('TERM', 'htdocs/index.html')">> FIRE-GEM Terminal</div>
    </div>
    <div id="taskbar">
        <button id="start-btn" onclick="toggle()">START</button>
    </div>
    <script>
        function toggle() {
            const m = document.getElementById('menu');
            m.style.display = (m.style.display === 'block') ? 'none' : 'block';
        }
    </script>
</body>
</html>
